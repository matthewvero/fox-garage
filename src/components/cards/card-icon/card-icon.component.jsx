import React from 'react'
import { CardIconElement } from './card-icon.styles'

const CardIcon = ({icon}) => {
      return (
            <CardIconElement icon={icon}/>
      )
}

export default CardIcon
